import csv
from conversion import string_to_binary, binary_to_string


def parser(file: str) -> None:
    while True:
        args = input(f"{file}>")
        if args == '.exit':
            break
        
    
   


if __name__=='__main__':
    parser()